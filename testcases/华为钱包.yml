
config:
    name: "demo testcase"
#    variables:
#        device_sn: "ABC"
#        username: ${ENV(USERNAME)}
#        password: ${ENV(PASSWORD)}
#    base_url: "http://127.0.0.1:5000"

teststeps:
-
    name: 华为钱包
    api: api/单接口调试用的/华为钱包.yml
#    variables:
#        user_agent: 'iOS/10.3'
#        device_sn: $device_sn
#    extract:
#        - token: content.token
    validate:
        - eq: ["status_code", 200]
#-
#    name: demo step 2
#    api: path/to/api2.yml
#    variables:
#        token: $token
